# Base URL of the FastAPI backend (include the /api/v1 prefix)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# Optional API key. Set this to match one of the backend's API_KEYS when the
# backend has authentication enabled. Sent as the X-API-Key header on requests.
NEXT_PUBLIC_API_KEY=
